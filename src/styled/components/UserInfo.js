import React from 'react'
import styled from 'styled-components'

export default styled.article`
  display: flex;
  align-items: center;
  
  .user-info {
    &__name {
      color: #555;
      margin: 0 10px;
    }
  }
`
